<% title "Questions" %>

<table>
  <tr>
    <th>Test Question</th>
    <th>Question</th>
  </tr>
  <% for question in @questions %>
    <tr>
      <td><%= question.test_question_id %></td>
      <td><%= question.question %></td>
      <td><%= link_to "Show", question %></td>
      <td><%= link_to "Edit", edit_question_path(question) %></td>
      <td><%= link_to "Destroy", question, :data => { :confirm => 'Are you sure?' }, :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Question", new_question_path %></p>
